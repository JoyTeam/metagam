var examRules;
var examAccept;

function showExam()
{
    var submitForm = document.getElementById('submitForm');
    var examForm = document.getElementById('examForm');
    if (!examRules) {

    }
}
