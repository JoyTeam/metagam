#!/usr/bin/python2.6

from mg.srv.server import Server
from mg.srv.common import Instance
from concurrence import dispatch

def main():
    inst = Instance()
    server = Server(inst)
    server.run()

dispatch(main)

